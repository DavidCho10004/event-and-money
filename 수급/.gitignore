# 자동 수집 캐시 (실행 시 네이버 금융에서 재수집됨)
data/*.csv

# 원본 누적 저장소 — 로컬 전용 (Railway는 data/processed/의 CSV만 읽음)
data/snapshots/
data/flows/
data/_checkpoints/

# 주간 실행 로그 (pykrx가 로그인 ID를 출력하므로 커밋 금지)
logs/

# Python
__pycache__/
*.py[cod]

# 친구분 프로토타입 엑셀 (개인 파일)
TalkFile*.xlsx
